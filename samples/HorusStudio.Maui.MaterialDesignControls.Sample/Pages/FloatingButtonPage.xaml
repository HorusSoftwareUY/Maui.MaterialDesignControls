<?xml version="1.0" encoding="utf-8" ?>
<local:BaseContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                       xmlns:material="clr-namespace:HorusStudio.Maui.MaterialDesignControls;assembly=HorusStudio.Maui.MaterialDesignControls"
                       xmlns:local="clr-namespace:HorusStudio.Maui.MaterialDesignControls.Sample.Pages"
                       xmlns:vm="clr-namespace:HorusStudio.Maui.MaterialDesignControls.Sample.ViewModels"
                       x:Class="HorusStudio.Maui.MaterialDesignControls.Sample.Pages.FloatingButtonPage"
                       x:TypeArguments="vm:FloatingButtonViewModel"
                       x:DataType="vm:FloatingButtonViewModel">

    <ScrollView>
        <VerticalStackLayout Style="{StaticResource MainLayout}">
            <material:MaterialLabel Text="{Binding Subtitle}" />

            <material:MaterialFloatingButton
                Icon="horus_logo"
                Type="{Binding ButtonType}"
                Position="{Binding PositionButton}"
                Command="{Binding FloatingButtonActionCommand}"
                IsVisible="{Binding IsVisible}" />

            <material:MaterialLabel Style="{StaticResource SectionTitle}" Text="Visibility" />
            <Frame Style="{StaticResource SurfaceContainer}" Padding="16,32">
                <HorizontalStackLayout HorizontalOptions="Center">
                    <material:MaterialLabel
                            Text="Is Visible"
                            VerticalTextAlignment="Center" />
                    <Switch IsToggled="{Binding IsVisible}" />
                </HorizontalStackLayout >
            </Frame>

            <material:MaterialLabel Style="{StaticResource SectionTitle}" Text="Type &amp; Position" />
            <Frame Style="{StaticResource SurfaceContainer}" Padding="16,32">
                <VerticalStackLayout>
                    <material:MaterialButton
                         Text="{Binding ButtonTextMove}"
                         Command="{Binding MoveButtonCommand}"/>
                    
                    <material:MaterialButton
                        Text="{Binding ButtonTextType}"
                        Command="{Binding ChangeTypeFloatingButtonCommand}"/>
                </VerticalStackLayout >
            </Frame>
        </VerticalStackLayout>
    </ScrollView>
</local:BaseContentPage>