<?xml version="1.0" encoding="utf-8"?>
<ContentView
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:material="clr-namespace:HorusStudio.Maui.MaterialDesignControls;assembly=HorusStudio.Maui.MaterialDesignControls"
    xmlns:vm="clr-namespace:HorusStudio.Maui.MaterialDesignControls.Sample.ViewModels"
    x:Class="HorusStudio.Maui.MaterialDesignControls.Sample.Views.PageWithTopAppBar"
    x:DataType="vm:BaseViewModel">
    <Grid
        RowDefinitions="Auto,*">
        <material:MaterialTopAppBar
            Headline="{Binding Title}"
            Description="{Binding Subtitle}"
            LeadingIconCommand="{Binding GoBackCommand}"
            LeadingIconIsBusy="{Binding Path=GoBackCommand.IsRunning}"
            LeadingIcon="ic_back.png"
            Type="Large"
            ScrollViewName="MdcPageScroll" />
        <ScrollView Grid.Row="1" x:Name="MdcPageScroll">
            <VerticalStackLayout Style="{StaticResource MainLayout}">
                <ContentPresenter
                    x:Name="contentPresenter" />
            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentView>