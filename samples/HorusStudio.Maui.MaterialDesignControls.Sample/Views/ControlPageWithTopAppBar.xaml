<?xml version="1.0" encoding="utf-8"?>
<ContentView
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:material="clr-namespace:HorusStudio.Maui.MaterialDesignControls;assembly=HorusStudio.Maui.MaterialDesignControls"
    xmlns:vm="clr-namespace:HorusStudio.Maui.MaterialDesignControls.Sample.ViewModels"
    x:Class="HorusStudio.Maui.MaterialDesignControls.Sample.Views.ControlPageWithTopAppBar"
    x:DataType="vm:BaseViewModel">
    <Grid
        RowDefinitions="Auto,*">
        <material:MaterialTopAppBar
            x:Name="topAppBar"
            AutomationId="topAppBar"
            Headline="{Binding Title}"
            Description="{Binding Subtitle}"
            LeadingIconCommand="{Binding GoBackCommand}"
            LeadingIconIsBusy="{Binding Path=GoBackCommand.IsRunning}"
            LeadingIcon="ic_back.png"
            Type="Large"
            TrailingIcons="{Binding ContextualActions}" />
        <ScrollView
            Grid.Row="1"
            x:Name="MdcControlPageScroll">
            <VerticalStackLayout
                Style="{StaticResource MainLayout}">
                <!-- <material:MaterialSegmentedButton -->
                <!--     ItemsSource="{Binding TabItems}" -->
                <!--     SelectedItem="{Binding SelectedTabItem}" -->
                <!--     SelectionCommand="{Binding TabItemsSelectionCommand}" -->
                <!--     HeightRequest="40" -->
                <!--     CornerRadius="20" /> -->
                <!-- <Grid> -->
                <!--     <ContentView -->
                <!--         x:Name="contentPresenter" -->
                <!--         IsVisible="{Binding IsCustomize, Converter={StaticResource ReverseBoolConverter}}" /> -->
                <!--     <material:MaterialCard -->
                <!--         Style="{StaticResource CardControlContainer}" -->
                <!--         IsVisible="{Binding IsCustomize}" -->
                <!--         Padding="32" -->
                <!--         Margin="0,24"> -->
                <!--         <material:MaterialLabel -->
                <!--             Text="Soon you will be able to customize controls live" -->
                <!--             HorizontalOptions="Center" -->
                <!--             VerticalOptions="Center" /> -->
                <!--     </material:MaterialCard> -->
                <!-- </Grid> -->
                
                <ContentView
                    x:Name="contentPresenter"
                    Padding="0,-16,0,0"/>
            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentView>