name: Build, sign and publish the sample app to App Store and Google Play

on:
  # push:
  #   branches: [ "feature/sample-app-release" ] # branches: [ "master" ]
  workflow_dispatch:
    inputs:
      platforms:
        description: "Platforms to build"
        required: true
        default: "android_ios"
        type: choice
        options:
          - android_ios
          - android
          - ios

env:
  SLN_FILE_PATH: 'HorusStudio.Maui.MaterialDesignControls.sln'
  CSPROJ_FILE_PATH: 'samples/HorusStudio.Maui.MaterialDesignControls.Sample/HorusStudio.Maui.MaterialDesignControls.Sample.csproj'

jobs:
  build:
    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v3

    - name: Show selected platforms to build
      run: |
        echo "Selected platforms to build: ${{ github.event.inputs.platforms }}"

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '9.0.x'
        include-prerelease: true

    # - name: Install MAUI workloads
    #   run: |
    #     echo "📦 Installing MAUI workloads..."
    #     dotnet workload install maui
    #     dotnet workload restore
    #     echo "✅ MAUI workloads installed successfully!"

    - name: Install MAUI workloads (Xcode 16.x compatible)
      run: |
        echo "📦 Installing MAUI workloads compatible with Xcode 16.x..."
        dotnet workload install maui --skip-manifest-update
        dotnet workload update --from-rollback-file https://aka.ms/dotnet/maui/rollback/9.0.100.json
        dotnet workload restore
        echo "✅ MAUI workloads installed successfully!"

    - name: Restore dependencies
      run: |
        echo "📦 Restoring dependencies..."
        dotnet restore ${{ env.SLN_FILE_PATH }}
        echo "✅ Dependencies restored successfully!"

    - name: Get version from nuget.props
      id: get_version
      run: |
        echo "📦 Extracting version from nuget.props..."
        VERSION=$(xmllint --xpath "string(//Project/PropertyGroup/Version)" nuget.props)
        echo "Version found: $VERSION"
        echo "version=$VERSION" >> $GITHUB_OUTPUT
        echo "✅ Version extracted successfully!"

    - name: Get release notes from nuget.props
      id: get_release_notes
      run: |
        echo "📦 Extracting release notes from nuget.props..."
        RELEASE_NOTES=$(xmllint --xpath "string(//Project/PropertyGroup/PackageReleaseNotes)" nuget.props)
        echo "Release notes found:"
        echo "$RELEASE_NOTES"
        echo "release_notes<<EOF" >> $GITHUB_OUTPUT
        echo "$RELEASE_NOTES" >> $GITHUB_OUTPUT
        echo "EOF" >> $GITHUB_OUTPUT
        echo "✅ Release notes extracted successfully!"

    # Android Build & Upload
    - name: Setup Firebase config file for Android
      if: ${{ github.event.inputs.platforms == 'android_ios' || github.event.inputs.platforms == 'android' }}
      run: |
        echo "📦 Creating Firebase config file for Android..."
        mkdir -p samples/HorusStudio.Maui.MaterialDesignControls.Sample/Platforms/Android
        echo "${{ secrets.FIREBASE_GOOGLE_SERVICES_JSON }}" > samples/HorusStudio.Maui.MaterialDesignControls.Sample/Platforms/Android/google-services.json
        echo "🔎 samples/HorusStudio.Maui.MaterialDesignControls.Sample/Platforms/Android/google-services.json (show 10 first lines):"
        head -n 10 samples/HorusStudio.Maui.MaterialDesignControls.Sample/Platforms/Android/google-services.json
        echo "✅ Firebase config file created successfully!"

    - name: Decode Android Keystore
      if: ${{ github.event.inputs.platforms == 'android_ios' || github.event.inputs.platforms == 'android' }}
      run: |
        echo "📦 Decoding Android Keystore..."
        echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 --decode > mauimaterialdesigncontrolssample.keystore
        echo "✅ Android Keystore decoded successfully!"

    - name: Set keystore permissions
      if: ${{ github.event.inputs.platforms == 'android_ios' || github.event.inputs.platforms == 'android' }}
      run: chmod 600 ./mauimaterialdesigncontrolssample.keystore

    - name: Build Android
      if: ${{ github.event.inputs.platforms == 'android_ios' || github.event.inputs.platforms == 'android' }}
      run: |
        echo "📦 Building Android app..."
        dotnet publish ${{ env.CSPROJ_FILE_PATH }} -f net9.0-android -c Release -o ./output/android \
          /p:AndroidKeyStore=true \
          /p:AndroidSigningKeyStore=${{ github.workspace }}/mauimaterialdesigncontrolssample.keystore \
          /p:AndroidSigningKeyAlias=${{ secrets.ANDROID_KEY_ALIAS }} \
          /p:AndroidSigningKeyPass=${{ secrets.ANDROID_KEY_PASSWORD }} \
          /p:AndroidSigningStorePass=${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
        echo "✅ Android app built successfully!"

    - name: Upload Android Artifact
      if: ${{ github.event.inputs.platforms == 'android_ios' || github.event.inputs.platforms == 'android' }}
      uses: actions/upload-artifact@v4
      with:
        name: HorusStudio.Maui.MaterialDesignControls.Sample.Android
        path: ./output/android/*.aab

    - name: Generate whatsnew files
      if: ${{ github.event.inputs.platforms == 'android_ios' || github.event.inputs.platforms == 'android' }}
      run: |
        mkdir -p whatsnew
        echo "🚀 This new version of the app corresponds to the latest available version of the NuGet package: https://www.nuget.org/packages/HorusStudio.Maui.MaterialDesignControls/${{ steps.get_version.outputs.version }}" > whatsnew/en-US.txt
        echo "Release notes for HorusStudio.Maui.MaterialDesignControls ${{ steps.get_version.outputs.version }}:" > whatsnew/en-US.txt
        echo "${{ steps.get_release_notes.outputs.release_notes }}" > whatsnew/en-US.txt

    # - name: Upload to Google Play
    #   if: ${{ github.event.inputs.platforms == 'android_ios' || github.event.inputs.platforms == 'android' }}
    #   uses: r0adkll/upload-google-play@v1
    #   with:
    #     serviceAccountJsonPlainText: ${{ secrets.GOOGLE_PLAY_JSON }}
    #     packageName: com.horusstudio.maui.materialdesigncontrols.sample
    #     releaseFiles: ./output/android/*.aab
    #     track: internal
    #     whatsNewDirectory: ./whatsnew

    # iOS Build & Upload
    - name: Setup Firebase config file for iOS
      if: ${{ github.event.inputs.platforms == 'android_ios' || github.event.inputs.platforms == 'ios' }}
      run: |
        echo "📦 Creating Firebase config file for iOS..."
        mkdir -p samples/HorusStudio.Maui.MaterialDesignControls.Sample
        echo "${{ secrets.FIREBASE_GOOGLE_SERVICE_INFO_PLIST }}" > samples/HorusStudio.Maui.MaterialDesignControls.Sample/GoogleService-Info.plist
        echo "🔎 samples/HorusStudio.Maui.MaterialDesignControls.Sample/GoogleService-Info.plist (show 10 first lines):"
        head -n 10 samples/HorusStudio.Maui.MaterialDesignControls.Sample/GoogleService-Info.plist
        echo "✅ Firebase config file created successfully!"

    - name: Setup Apple Certificate
      if: ${{ github.event.inputs.platforms == 'android_ios' || github.event.inputs.platforms == 'ios' }}
      uses: apple-actions/import-codesign-certs@v2
      with:
        p12-file-base64: ${{ secrets.P12_CERTIFICATE_BASE64 }}
        p12-password: ${{ secrets.P12_PASSWORD }}

    - name: Install Provisioning Profile
      if: ${{ github.event.inputs.platforms == 'android_ios' || github.event.inputs.platforms == 'ios' }}
      run: |
        echo "📦 Installing Provisioning Profile..."
        echo "${{ secrets.PROVISIONING_PROFILE_BASE64 }}" | base64 --decode > profile.mobileprovision
        mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
        cp profile.mobileprovision ~/Library/MobileDevice/Provisioning\ Profiles/
        echo "✅ Provisioning Profile installed successfully!"

    - name: Build iOS
      if: ${{ github.event.inputs.platforms == 'android_ios' || github.event.inputs.platforms == 'ios' }}
      run: |
        echo "📦 Building iOS app..."
        dotnet publish ${{ env.CSPROJ_FILE_PATH }} -f net9.0-ios -c Release -o ./output/ios \
        /p:ArchiveOnBuild=true \
        /p:CodesignKey="Apple Distribution"
        echo "✅ iOS app built successfully!"

    - name: Upload iOS Artifact
      if: ${{ github.event.inputs.platforms == 'android_ios' || github.event.inputs.platforms == 'ios' }}
      uses: actions/upload-artifact@v4
      with:
        name: HorusStudio.Maui.MaterialDesignControls.Sample.iOS
        path: ./output/ios/*.ipa

    # - name: Upload to TestFlight
    #   if: ${{ github.event.inputs.platforms == 'android_ios' || github.event.inputs.platforms == 'ios' }}
    #   uses: apple-actions/upload-testflight-build@v1
    #   with:
    #     app-path: ./output/ios/*.ipa
    #     issuer-id: ${{ secrets.APPLE_ISSUER_ID }}
    #     api-key-id: ${{ secrets.APPLE_KEY_ID }}
    #     api-private-key: ${{ secrets.APPLE_PRIVATE_KEY }}
    #     release-notes: |
    #       🚀 This new version of the app corresponds to the latest available version of the NuGet package: https://www.nuget.org/packages/HorusStudio.Maui.MaterialDesignControls/${{ steps.get_version.outputs.version }}
    #       Release notes for HorusStudio.Maui.MaterialDesignControls ${{ steps.get_version.outputs.version }}:
    #       ${{ steps.get_release_notes.outputs.release_notes }}
